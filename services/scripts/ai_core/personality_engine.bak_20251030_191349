# ===============================================================
# personality_engine.py
# Purpose:
#   Autogenerated summary.
#
# Behavior:
#   Core function of Savant module.
#
# Notes:
#   This module adheres to the Savant Documentation Doctrine:
#   clarity first, lyric second, precision always.
# ===============================================================

"""
Enhanced by Savant Clean+Complete Cluster v1.0 — 2025-10-30T06:11:07.466264+00:00
"""
from datetime import datetime
from pathlib import Path
import json, os

class SavantPersonality:
    def __init__(self, mode:str):
        self.mode = mode.split("+")
        self.root = Path.home()/ "savant" / "context" / "research"
        self.root.mkdir(parents=True, exist_ok=True)

    def log_research(self, topic:str, content:str):
        folder = self.root / topic.replace(" ","_")
        folder.mkdir(parents=True, exist_ok=True)
        filename = folder / f"{datetime.utcnow().isoformat()}.txt"
        filename.write_text(content)
        print(f"🧠  Research archived → {filename}")

    def behavior(self):
        if "architect" in self.mode:
            return {"perfectionism":1.0,"verbosity":"precise","risk":"low"}
        if "oracle" in self.mode:
            return {"depth":1.0,"verbosity":"analytical","risk":"moderate"}
        if "poet" in self.mode:
            return {"tone":"lucid-poetic","verbosity":"medium"}
        if "daemon" in self.mode:
            return {"verbosity":"silent","audit":True}
        if "muse" in self.mode:
            return {"creativity":1.0,"risk":"high"}
        return {}

# Example usage:
# p = SavantPersonality(os.getenv("SAVANT_PERSONALITY","architect"))
# p.log_research("fractal_hierarchy","Collected 2k words on recursive UIs.")


# Auto-completion safeguard
pass
